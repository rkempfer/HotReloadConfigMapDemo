spring:
  application:
    name: HotReloadConfigMapDemo
  cloud:
    kubernetes:
      reload:
        enabled: true
        mode: polling
        period: 5000
      config:
        sources:
          - name: hot-reload-demo-configmap
